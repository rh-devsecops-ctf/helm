apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ .Values.subscription.name }}-og
  namespace: {{ .Values.namespace }}
spec:
  # rhbk-operator does not support AllNamespaces; scope it to the operator namespace
  targetNamespaces:
    - {{ .Values.namespace }}