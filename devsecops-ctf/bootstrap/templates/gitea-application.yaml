---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $.Values.gitea.applicationName }}
  namespace: {{ $.Values.gitea.applicationNamespace }}
  annotations:
    {{- include "bootstrap.gitea.argocd-syncwave" $ | nindent 4 }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    name: ""
    namespace: {{ $.Values.gitea.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ $.Values.gitea.source.repoUrl }}
    targetRevision: {{ $.Values.gitea.source.targetRevision }}
    path: {{ $.Values.gitea.source.path }}