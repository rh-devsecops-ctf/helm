---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $.Values.gitea-operator.applicationName }}
  namespace: {{ $.Values.gitea-operator.applicationNamespace }}
  annotations:
    {{- include "bootstrap.gitea-operator.argocd-syncwave" $ | nindent 4 }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    name: ""
    namespace: {{ $.Values.gitea-operator.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ $.Values.gitea-operator.source.repoUrl }}
    targetRevision: {{ $.Values.gitea-operator.source.targetRevision }}
    path: {{ $.Values.gitea-operator.source.path }}