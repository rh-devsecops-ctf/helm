---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $.Values.argocd.tpaDB.applicationName }}
  namespace: {{ $.Values.argocd.tpaDB.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.argocd.tpaDB.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    name: ""
    namespace: {{ $.Values.argocd.tpaDB.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ $.Values.argocd.tpaDB.source.repoUrl }}
    targetRevision: {{ $.Values.argocd.tpaDB.source.targetRevision }}
    path: {{ $.Values.argocd.tpaDB.source.path }}
    helm:
      valueFiles:
        - ../trusted-profile-analyzer-db/values.yaml