---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $.Values.argocd.keycloak-db.applicationName }}
  namespace: {{ $.Values.argocd.keycloak-db.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.argocd.keycloak-db.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    name: ""
    namespace: {{ $.Values.argocd.keycloak-db.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ $.Values.argocd.keycloak-db.source.repoUrl }}
    targetRevision: {{ $.Values.argocd.keycloak-db.source.targetRevision }}
    path: {{ $.Values.argocd.keycloak-db.source.path }}
    helm:
      valueFiles:
        - ../keycloak-db/values.yaml