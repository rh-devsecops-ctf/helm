---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $.Values.argocd.minio.applicationName }}
  namespace: {{ $.Values.argocd.minio.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.argocd.minio.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    name: ""
    namespace: {{ $.Values.argocd.minio.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ $.Values.argocd.minio.source.repoUrl }}
    targetRevision: {{ $.Values.argocd.minio.source.targetRevision }}
    path: {{ $.Values.argocd.minio.source.path }}
    helm:
      parameters:
        - name: subDomain
          value: {{ $.Values.bootstrap.subDomain | default "" | quote }}    
      valueFiles:
        - ../values.yaml