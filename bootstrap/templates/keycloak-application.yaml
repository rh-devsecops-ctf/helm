---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $.Values.argocd.keycloak.applicationName }}
  namespace: {{ $.Values.argocd.keycloak.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.argocd.keycloak.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    name: ""
    namespace: {{ $.Values.argocd.keycloak.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ $.Values.argocd.keycloak.source.repoUrl }}
    targetRevision: {{ $.Values.argocd.keycloak.source.targetRevision }}
    path: {{ $.Values.argocd.keycloak.source.path }}
    helm:
      parameters:
        - name: subDomain
          value: {{ $.Values.subDomain | default "" | quote }}    
      valueFiles:
        - ../values.yaml